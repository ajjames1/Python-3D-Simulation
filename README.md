# Python-3D-Simulation
How It Works
- Uses tkinter for the GUI.
- Embeds a 3D cube using matplotlib and mpl_toolkits.mplot3d.
- Defines vertices and faces to construct the cube.
- Displays the cube inside a tkinter window.
